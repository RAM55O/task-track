apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tasktrack.fullname" . }}-test-connection"
  labels:
    {{- include "tasktrack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tasktrack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
