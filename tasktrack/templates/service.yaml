apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasktrack.fullname" . }}
  labels:
    {{- include "tasktrack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tasktrack.selectorLabels" . | nindent 4 }}
