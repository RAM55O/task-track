apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tasktrack-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/RAM55O/task-track.git"
    targetRevision: main
    path: tasktrack
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true # Adopts existing pods and HPA without deleting them
